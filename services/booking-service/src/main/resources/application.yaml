server:
  port: 5009

spring:
  application:
    name: booking-service

  datasource:
    url: jdbc:h2:mem:booking_service
    username: we
    password: we
    driver-class-name: org.h2.Driver
    h2:
      console:
        enabled: true
        path: /h2-console
    jpa:
      hibernate:
        ddl-auto: update

  #  datasource:
  #    url: jdbc:mysql://localhost:3306/booking_service
  #    username: we
  #    password: we
